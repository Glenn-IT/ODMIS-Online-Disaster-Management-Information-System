<IfModule mod_rewrite.c>
    RewriteEngine On
    # Pass Authorization Header to PHP under Apache / FastCGI
    RewriteCond %{HTTP:Authorization} ^(.*)
    RewriteRule .* - [e=HTTP_AUTHORIZATION:%1]
</IfModule>
